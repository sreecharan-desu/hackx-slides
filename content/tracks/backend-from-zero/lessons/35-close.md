---
title: "35. That's the job"
order: 35
---

# That's the job

You made an API that lets people join — laptop `.env`, live `.env` the same keys via Actions (not Neon alone). Door secrets are only SSH. Health can lie. Kill the box before dinner.

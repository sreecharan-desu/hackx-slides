---
title: "35. You can explain this now"
order: 35
---

# You can explain this now

You can point at the sketch, name every folder, and ship the same repo to a public IP. Laptop `.env` and live `.env` are copies of the same keys. Actions is not magic — it SSHs and writes the file. `/chat` and `/ask` are two doors on one lock. Health can look fine while register is 500. Stop the EC2 instance when you are done. Don't leave it billing overnight.
